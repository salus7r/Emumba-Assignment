import styled from "styled-components";

import { EmumbaCard } from "@components/styled";

export const Wrapper = styled(EmumbaCard)`
  padding: 1.25rem;
`;

export const WeeklyContent = styled(EmumbaCard)``;
